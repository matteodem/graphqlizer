import {
  resolvers,
  resolver,
} from '../lib'
import {
  defaultCrud,
  fieldsConfig,
  inputSimpleSchema,
  fakeCollectionForResolvers,
} from './testData'

Tinytest.add('Graphqlizer - Public API - resolver', function (test) {
  //test.equal(resolver.create('Alien', ))
})
